import Vue from 'vue'
import Router from 'vue-router'
import Home from '@/components/Home'
import Home from '@/components/Auth/Login'
import Home from '@/components/Auth/Registration'
Vue.use(Router)
export default new Router({
// eslint-disable-next-line
})